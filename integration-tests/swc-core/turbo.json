{
   "$schema": "https://turbo.build/schema.json",
   "extends": ["//"],
   "pipeline": {
      "test": {
         "dependsOn": ["^build"],
         "inputs": [
            "src/**/*.ts",
            ".swcrc",
            "tsconfig.json"
         ],
         "outputs": ["dist/**"],
         "outputMode": "errors-only"
      }
   }
}
